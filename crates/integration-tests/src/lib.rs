//! Integration test crate — no public API.
//!
//! All tests live in the `tests/` subdirectory and are compiled as separate
//! test binaries by Cargo. This `lib.rs` exists only to satisfy Cargo's crate
//! layout; it has no public items of its own.
